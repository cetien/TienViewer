<UserControl x:Class="TienViewer.Viewers.MediaViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 미디어 영역 -->
        <MediaElement x:Name="Media" Grid.Row="0"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Stop"
                      Stretch="Uniform"
                      MediaOpened="Media_MediaOpened"
                      MediaFailed="Media_MediaFailed"
                      MediaEnded="Media_MediaEnded"/>

        <!-- 컨트롤 바 -->
        <Border Grid.Row="1" Background="#CC1A1A1A" Padding="8,6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 시크 바 -->
                <Slider x:Name="SeekBar" Grid.Row="0"
                        Minimum="0" Maximum="1" Value="0"
                        Margin="0,0,0,6"
                        PreviewMouseDown="SeekBar_PreviewMouseDown"
                        PreviewMouseUp="SeekBar_PreviewMouseUp"/>

                <!-- 버튼 행 -->
                <DockPanel Grid.Row="1">
                    <Button x:Name="BtnPlayPause" Content="▶" Width="36"
                            Click="BtnPlayPause_Click" DockPanel.Dock="Left" Margin="0,0,6,0"/>
                    <Button Content="■" Width="36"
                            Click="BtnStop_Click" DockPanel.Dock="Left" Margin="0,0,12,0"/>

                    <TextBlock x:Name="TimeText" VerticalAlignment="Center"
                               Foreground="#CCCCCC" FontSize="11" FontFamily="Consolas"
                               DockPanel.Dock="Left" Margin="0,0,12,0"
                               Text="0:00 / 0:00"/>

                    <!-- 볼륨 -->
                    <TextBlock Text="🔊" VerticalAlignment="Center"
                               DockPanel.Dock="Right" Margin="6,0,4,0"/>
                    <Slider x:Name="VolumeBar" Width="80"
                            Minimum="0" Maximum="1" Value="0.7"
                            VerticalAlignment="Center"
                            DockPanel.Dock="Right"
                            ValueChanged="VolumeBar_ValueChanged"/>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
